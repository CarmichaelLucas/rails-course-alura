<p id="notice"><%= notice %></p>

<h1>Departamento</h1>

<table class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>Nome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @departments.each do |department| %>
      <tr>
        <td><%= department.name %></td>
        <td><%= link_to 'Detalhes', department, class:"btn btn-default" %></td>
        <td><%= link_to 'Editar', edit_department_path(department), class:"btn btn-default" %></td>
        <td><%= link_to 'Remover', department, method: :delete, data: { confirm: 'Você deseja remover?' }, class:"btn btn-default" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Departamento', new_department_path, class:"btn btn-default" %>
